<section class="docs-empty">
  <h1>No documentation pages found.</h1>
  <p>Add markdown files under <code>src/lib/content/docs</code>.</p>
</section>

<style>
  .docs-empty {
    border: var(--border-width-divider, var(--border-1)) dashed var(--border-color-default);
    border-radius: var(--radius-container);
    padding: var(--spacing-comfy, var(--spacing-6));
    background: var(--background-panel);
    box-shadow: var(--elevation-card);
  }

  h1 {
    margin: 0 0 var(--spacing-gap-small, var(--spacing-2)) 0;
    font-family: var(--type-heading-family);
    font-size: var(--type-heading-size);
    font-weight: var(--type-heading-weight);
    line-height: var(--type-heading-leading);
    letter-spacing: var(--type-heading-tracking);
    color: var(--on-surface-strong);
  }

  p {
    margin: 0;
    font-family: var(--type-body-family);
    font-size: var(--type-body-size);
    line-height: var(--type-body-leading);
    color: var(--text-muted);
  }

  code {
    font-family: var(--type-code-family);
    font-size: var(--type-code-size);
  }
</style>
